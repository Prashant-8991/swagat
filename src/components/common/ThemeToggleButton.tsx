//@ts-nocheck
import { useTheme } from "../../context/ThemeContext";

export const ThemeToggleButton: React.FC = () => {
    // Dark mode is disabled application-wide.
    // This component now renders nothing to prevent users from trying to toggle the theme.
    return null;
};
